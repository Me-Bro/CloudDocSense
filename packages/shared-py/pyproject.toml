[project]
name = "docsense-shared"
version = "0.1.0"
description = "Shared prompts, schemas, and eval helpers for DocSense"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
